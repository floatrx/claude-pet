{
  "name": "claude-pet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tsc && vite build",
    "build:app": "tauri build",
    "build:dmg": "tauri build",
    "build:debug": "tauri build --debug",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs",
    "icons:tray": "node scripts/generate-tray-icon.mjs",
    "typecheck": "tsc -b",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
